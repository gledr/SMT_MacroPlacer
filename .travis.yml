language: cpp
os: linux
sudo: required

jobs:
    include:
        - os: linux
          dist: focal
          compiler: g++
          addons:
            apt:
              packages:
                - cmake
                - git
                - g++
                - libboost-program-options-dev
                - libboost-filesystem-dev
                - doxygen
                - graphviz
                - bison
                - zlib1g-dev
                - python2-dev
                - python-numpy
              
install:
  - echo "Install"

before_script:
  - mkdir -p build && cd build
  - cmake ..
  - make
    
script:
  - echo "After Script"
    
after_success:
 - echo 
